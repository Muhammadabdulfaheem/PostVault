import Foundation

enum TabScreenAccessibility {
    enum Posts {
        static let table = "posts.table"
        static let loader = "posts.loader"
    }

    enum Favorites {
        static let table = "favorites.table"
        static let emptyState = "favorites.emptyState"
    }
}
