import Foundation
enum LoginAccessibility {
    enum ID {
        static let emailField = "login.emailField"
        static let passwordField = "login.passwordField"
        static let submitButton = "login.submitButton"
        static let loader = "login.loader"
    }
}
